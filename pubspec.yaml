name: book_library
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0  # ✅ update to the latest stable
  firebase_database: ^10.4.0  # ✅ update to the latest stable
  url_launcher: ^6.2.5  # ✅ update to the latest stable
  ## this is for the download
  dio: ^5.4.0
  path_provider: ^2.1.2
  open_file: ^3.3.2
  permission_handler: ^11.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

